AOS.init({
  offset: 120, // Jarak sebelum elemen mulai dianimasikan (default: 120px)
  delay: 200, // Penundaan sebelum animasi dimulai (default: 0ms)
  duration: 1000, // Durasi animasi dalam milidetik (default: 400ms)
  easing: "ease-in-out", // Jenis transisi (default: 'ease')
  once: true, // Animasi hanya terjadi sekali (true) atau setiap scroll (false)
  mirror: false, // Apakah animasi terjadi saat menggulir ke atas kembali
});
